# Models

여기는 비어 있는 게 정상이다. 가중치는 git에 넣지 않는다(142MB, 재생성 가능).

```bash
./Tools/fetch_models.sh
```

받고 나면:

```
DepthAnythingV2SmallF16.mlpackage   원본 (Apache-2.0, apple/coreml-depth-anything-v2-small)
DepthAnythingV2SmallF16.mlmodelc    Core ML 컴파일본 — Core ML 경로 벤치용
DepthAnythingV2Small.mtlpackage     Metal 4 ML 인코더용 — 앱이 실제로 쓰는 것
```
